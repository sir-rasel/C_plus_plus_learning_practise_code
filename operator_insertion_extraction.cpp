#include <iostream>
using namespace std;

int main(){
    string s;
    cout<<"Inserting a name by insertion operator: ";
    cin>>s;
    cout<<"Extracting in console the name by Extraction operator : "<<s<<endl;
    return 0;
}
